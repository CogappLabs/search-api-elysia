pre-commit:
  piped: true
  commands:
    biome:
      glob: "*.{ts,js,json}"
      run: bunx @biomejs/biome check --no-errors-on-unmatched {staged_files}
    typecheck:
      run: bunx tsc --noEmit
    build-types:
      glob: "src/**/*.ts"
      run: bun run build:types && git add dist/
